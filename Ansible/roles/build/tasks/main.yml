---
# build project
       - name: build app
         shell: npm install && ng build --prod && chmod 777 -R /var/lib/jenkins/workspace/devops/
         # npm install && npm run build --prod && chmod 777 -R /var/lib/jenkins/workspace/devops/
